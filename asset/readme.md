# 这个目录存放实际运行图片
- 1.png  实际使用`/test_client/server.py`程序，监控收到的rk3566上传的图片

- 2.png rk3566检测到人的时候上传的原图片

- static_load.png 纯实时显示（摄像头采集 + 实时显示），使用top命令查看CPU负载信息

- static_load.png 满血运行（摄像头采集 + 实时显示 + NPU 目标检测 + 画框 + 保存到本地+上云），使用top命令查看CPU负载信息

